name: Protocol Feedback
description: Feedback on the protocol design, interaction model, trust framework, whitepaper, or technical-specification set.
title: "[Feedback] "
labels: ["feedback"]
body:
  - type: markdown
    attributes:
      value: |
        Thank you for taking the time to provide feedback on the Agentic Credit Broking Protocol.
  - type: dropdown
    id: area
    attributes:
      label: Area
      description: Which part of the protocol does this relate to?
      options:
        - Technical specification
        - REST binding
        - MCP binding
        - Schema appendix
        - Standards and conventions
        - Goals and scope
        - Whitepaper / conceptual model
        - Interaction model (§4)
        - Broker action types (§5)
        - Domain entity vocabulary (§6)
        - Trust model (§7)
        - Benchmarking (§8)
        - AI assistants as User Agents (§9)
        - Roles and responsibilities (§3)
        - General / cross-cutting
    validations:
      required: true
  - type: textarea
    id: feedback
    attributes:
      label: Feedback
      description: Your feedback. Be as specific as possible. Reference documents and section numbers where relevant.
    validations:
      required: true
  - type: textarea
    id: context
    attributes:
      label: Context
      description: Any relevant context — your perspective (e.g. broker, UA developer, regulator), related work, or examples.
